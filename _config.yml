# Site settings
title: CV
description: CV

plugins:
  - jekyll-seo-tag

# Build settings
markdown: kramdown
exclude: ["README.md", "bin", "Gemfile", "Gemfile.lock", "claude-thoughts"]
sass:
  style: compressed
